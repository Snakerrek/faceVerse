# =========================================================================
# General
# =========================================================================

# Logs and databases
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.sqlite
*.sqlite3
*.db-journal # General SQLite journal files

# OS generated files
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
*.stackdump

# Temporary files
*~
*.swp
*.swo

# =========================================================================
# IDE / Editor specific
# =========================================================================

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains IDEs (PyCharm, WebStorm, IntelliJ, etc.)
.idea/
*.iml

# Sublime Text
*.sublime-project
*.sublime-workspace

# NetBeans
nbproject/

# =========================================================================
# Python / Flask (Server specific - inside 'server/' folder)
# =========================================================================
__pycache__/
server/*.py[cod]
server/.env* # Environment variables in server folder
server/instance/ # Flask instance folder
server/faceVerse.db # Specific SQLite database file in server folder
server/*.sqlite*-journal # Specific journal files in server folder

# Python Virtual Environments (if created inside server)
server/.venv/
server/venv/
server/env/
server/ENV/

# Python build / distribution / testing artifacts (inside server)
server/build/
server/develop-eggs/
server/dist/
server/downloads/
server/eggs/
server/.eggs/
server/lib/
server/lib64/
server/parts/
server/sdist/
server/var/
server/wheels/
server/pip-wheel-metadata/
server/share/python-wheels/
server/*.egg-info/
server/.installed.cfg
server/*.egg
server/.pytest_cache/
server/.coverage*
server/htmlcov/
server/.mypy_cache/

# =========================================================================
# Node / React Native / TypeScript (Client specific - inside 'client/' folder)
# =========================================================================

# Node dependencies
client/node_modules/

# TypeScript build info
client/.tsbuildinfo

# React Native specific builds/caches
client/ios/Pods/
client/ios/build/
client/android/build/
client/android/app/build/
client/android/.gradle/
client/.expo/
client/.expo-shared/
client/web-build/ # Expo web build output

# Bundler caches / outputs
client/metro-cache/
# Uncomment if you don't want generated bundles in git (often needed for deployment/testing though)
# client/*.bundle

# Debugging
client/npm-debug.log*
client/yarn-debug.log*
client/yarn-error.log*

# Testing / Coverage (client specific)
client/coverage/

# Environment variables (client specific)
client/.env*
client/.env.*.local

# Editor-specific for JS/TS projects in client
# (Some might be redundant with global IDE settings above)
client/.eslintcache

# Expo specific credentials/keys (NEVER commit these)
client/*.jks
client/*.p8
client/*.p12
client/*.key
client/credentials.json
client/google-services.json # Usually downloaded, not committed
client/GoogleService-Info.plist # Usually downloaded, not committed

# =========================================================================
# Other common ignores
# =========================================================================
.watchmanconfig # Watchman config file

